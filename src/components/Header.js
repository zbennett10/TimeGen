import React, {Component} from 'react'

export default (props) => {
    return (
        <div className="jumbotron text-center">
            <h1>TimeGen</h1>
            <nav className="nav navbar-toggleable-md navbar-light bg-faded">
                <a className="nav-item btn btn-md">Home</a>
                <a className="nav-item btn btn-md">Sign in</a>
            </nav>
        </div>
    );
}